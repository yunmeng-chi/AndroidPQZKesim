<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:opencv="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#FFFFFF">

    <TextView
        android:id="@+id/tv_step_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="步骤 1/2：生物特征录入"
        android:textColor="#2563EB"
        android:textStyle="bold"
        android:layout_marginBottom="10dp"/>

    <TextView
        android:id="@+id/tv_status_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="请点击下方按钮开始采集人脸"
        android:textAlignment="center"
        android:textColor="#4B5563"
        android:layout_marginBottom="10dp"/>

    <FrameLayout
        android:id="@+id/layout_step_face"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:visibility="visible">

        <org.opencv.android.JavaCameraView
            android:id="@+id/register_camera_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            opencv:camera_id="front"
            opencv:show_fps="false"/>

        <View
            android:layout_width="260dp"
            android:layout_height="260dp"
            android:layout_gravity="center"
            android:background="@drawable/face_scan_frame"/>
    </FrameLayout>

    <LinearLayout
        android:id="@+id/layout_step_init"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:indeterminateTint="#2563EB"/>
        <TextView
            android:id="@+id/tv_init_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="正在初始化安全芯片并生成公钥 T..."
            android:layout_marginTop="16dp"
            android:textColor="#6B7280"
            android:textAlignment="center"/>
    </LinearLayout>

    <Button
        android:id="@+id/btn_next_step"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="开始采集"
        android:background="@drawable/btn_primary_gradient"
        android:textColor="#FFFFFF"
        android:layout_marginTop="20dp"
        android:enabled="true"/>
</LinearLayout>