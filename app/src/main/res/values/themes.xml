<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PQZKEsim" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#1677FF</item>
        <item name="colorPrimaryVariant">#0D5ECC</item>
    </style>

    <style name="Theme.PQZKEsim" parent="Base.Theme.PQZKEsim" />

    <!-- BottomNavigationView 文字样式：紧凑排版，图标与文字间距小 -->
    <style name="BottomNavTextActive">
        <item name="android:textSize">11sp</item>
        <item name="android:textColor">#1677FF</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:lineSpacingMultiplier">0.9</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="BottomNavTextInactive">
        <item name="android:textSize">11sp</item>
        <item name="android:textColor">#9CA3AF</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:lineSpacingMultiplier">0.9</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- ActivationSuccessDialog 窗口动画 -->
    <style name="ActivationSuccessDialogAnimation" parent="android:style/Theme.Dialog">
        <item name="android:windowEnterAnimation">@anim/dialog_fade_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_fade_out</item>
    </style>

    <!-- ActivationSuccessDialog 全屏透明主题：无背景、无 dim、无标题栏 -->
    <style name="TransparentDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>
</resources>